#include <iostream>
#include <Particle2D.h>

using namespace std;

Particle2D::Particle2D(float posX, float posY, float velX, float velY, float m) : position(posX, posY), velocity(velX, VelY), mass(n)
